"use client"
import { useGApp } from "@/hooks";

export default function Page() {
    const gapp = useGApp();
    
    return (
        <div>
            <h1>Signup Finish</h1>
        </div>
    )
}   